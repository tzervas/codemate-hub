version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace
    environment:
      - ENV_VAR_NAME=value
    ports:
      - "3000:3000"
    networks:
      - dev-network

networks:
  dev-network:
    driver: bridge