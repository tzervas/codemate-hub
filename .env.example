# Environment Configuration Template
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: .env is in .gitignore and should NEVER be committed
# Sensitive production values should be set via environment or secrets management

# ============================================================================
# Required Variables
# ============================================================================

# Code-Server Password (REQUIRED)
# Used for authentication to the web-based VS Code instance
# Generate a secure password: openssl rand -base64 32
PASSWORD=your-secure-password-here

# ============================================================================
# Ollama Configuration
# ============================================================================

# Ollama API base URL (container network vs localhost)
# For container-to-container: http://ollama:11434
# For host-to-container: http://localhost:11434
OLLAMA_BASE_URL=http://ollama:11434

# Ollama Performance Tuning (CPU mode)
# Number of parallel requests the server can handle
OLLAMA_NUM_PARALLEL=4

# Number of threads to use for computation
OLLAMA_NUM_THREAD=4

# ============================================================================
# Chroma Database Configuration
# ============================================================================

# Directory path for Chroma vector database persistence
# Container path: /app/chroma_db (mapped to ./chroma_db on host)
CHROMA_DB_DIR=/app/chroma_db

# ============================================================================
# Optional: External API Keys
# ============================================================================

# OpenAI API Key (for GPT models)
# OPENAI_API_KEY=sk-...

# Anthropic API Key (for Claude models)
# ANTHROPIC_API_KEY=sk-ant-...

# ============================================================================
# Optional: Advanced Configuration
# ============================================================================

# Log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Langflow configuration
# LANGFLOW_CONFIG_DIR=/app/langflow_config
