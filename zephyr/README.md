# Zephyr enclaves (placeholder)

This directory is bind-mounted into the `app` container (`./zephyr:/app/zephyr`)
so future enclave/isolation work has a stable path.

**Status:** Not implemented (see `trackers/tasks/05-enclaves-zephyr.md` and
open planning in `trackers/PLAN.md`). Files here are intentionally minimal so
default deploys do not fail on a missing mount path.

Do not treat this tree as a working enclave runtime until Task 05 lands.
